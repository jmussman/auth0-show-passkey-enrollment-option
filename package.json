{
  "name": "auth0-show-passkey-enrollment-options",
  "version": "1.0.0",
  "requires": true,
  "type": "module",
  "packages": {},
  "devDependencies": {
    "@vitest/coverage-v8": "latest",
    "auth0": "latest",
    "ejs-lint": "latest",
    "vitest": "latest",
    "vitest-mock-commonjs": "latest"
  },
  "scripts": {
    "test": "vitest run --no-file-parallelism",
    "test-coverage": "vitest run --no-file-parallelism --coverage"
  }
}
